# Don't print a new line at the start of the prompt
add_newline = false
continuation_prompt = "[▸▹ ](dimmed white)"

# Use custom format
format = """
[╭─](white)$username$git_branch in $directory$package$cmd_duration
[╰─](white)$character """

right_format = """
$time"""

# Wait 10 milliseconds for starship to check files under the current directory.
scan_timeout = 10

# Username settings
[username]
show_always = true
style_root = "bold red"
style_user = "bold yellow"
format = "[codex]($style) "
disabled = false

# Hostname settings
[hostname]
ssh_only = false
format =  "on [$hostname](bold red)"
trim_at = ".companyname.com"
disabled = false

# Directory indicator
[directory]
read_only = "🔒"
style = "bold cyan"
read_only_style = "red"
truncation_length = 8
truncation_symbol = "…/"
format = "[$read_only]($read_only_style)[$path]($style) "
disabled = false

# Package Version Config
[package]
format = "[$version](208 bold) "

# Java settings
[java]
symbol = "☕ "
style = "red dimmed"
format = "via [${symbol}${version}]($style) "

# Python settings
[python]
symbol = "🐍 "
style = "yellow bold"
python_binary = "python3"
pyenv_version_name = true

# git settings
[git_branch]
symbol = "🌱 "

# Battery information
[battery]
full_symbol = "🔋"
charging_symbol = "⚡️"
discharging_symbol = "💀"

[[battery.display]]
threshold = 10
style = "bold red"

# cmd_duration config
[cmd_duration]
style = "bold yellow"
format = "⌚ [$duration]($style)"

# Character config
[character]
format = "$symbol"
success_symbol = "[ 💻](bold green)"
error_symbol = "[✗](bold red) "
disabled = false

[time]
disabled = false
format = "[ $time]($style)"
time_format = "%R"
utc_time_offset = "local"
style = "italic dimmed white"
